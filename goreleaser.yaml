build:
  main: ./cmd/datagen
  binary: datagen
  ldflags_template: -s -w -X main.build={{.Version}}
  goos:
    - windows
    - darwin
    - linux
  goarch:
    - amd64

brew:
  github:
    owner: aybabtme
    name: homebrew-tap

fpm:
  formats:
    - deb
